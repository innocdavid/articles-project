extends layout 

block content
    h1 #{title}
    ul.list-group 
        each article, i in articles  
            li.list-group-item 
                a.text-decoration-none(href='/article/'+article._id)  #{article.title}